version: 0.2
phases:
  install:
    commands:
      - pip install ansible
  build:
    commands:
      - cd "${CODEBUILD_SRC_DIR}/tests"
      - bash aws_run.sh
  post_build:
    commands:
      - echo "terrakvm online tests completed on `date`"
artifacts:
  files:
    - '**/*'
