all:
  vars:
    ansible_ssh_user: ${ssh_user}
    ansible_ssh_private_key_file: ${pkey_file_path}
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
  hosts:
{% for vm in vms %}
    {{ vm.vm_name }}:
      ansible_host: ${ {{ vm.vm_name }}_address }
{% endfor %}
